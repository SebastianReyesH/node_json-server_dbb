{
  "name": "proyecto-json-server",
  "version": "1.0.0",
  "description": "servidor de datos ",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "json-server --watch db.json"
  },
  "author": "Sebastian R H",
  "license": "ISC",
  "devDependencies": {
    "nodemon": "^3.1.9"
  },
  "dependencies": {
    "json-server": "^1.0.0-beta.3"
  }
}
